services:
  python-qr:
    container_name: python-qr
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    networks:
      - dockerprojects_default

networks:
  dockerprojects_default:
    external: true
