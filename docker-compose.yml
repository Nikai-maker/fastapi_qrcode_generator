services:
  python-qr:
    container_name: python-qr
    build: .
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    networks:
      - dockerprojects_default

networks:
  dockerprojects_default:
    external: true
